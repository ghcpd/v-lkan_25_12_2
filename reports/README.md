# Reports

- `pytest` is configured to emit JUnit XML to `reports/junit.xml`.
- Use `pytest --junitxml=reports/junit.xml` (default via `pytest.ini`).
- Template for manual test report: `reports/test_report_template.md`.
